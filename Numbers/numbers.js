const firstNumber = 50;
const secondNumber = 10;
let result;
//Simple math operations
result = firstNumber + secondNumber; //60
result = firstNumber - secondNumber; //40
result = firstNumber * secondNumber; //500
result = firstNumber / secondNumber; //5
result = firstNumber % secondNumber; //0

//Math object 
result = Math.PI; //3.14
result = Math.round(3.8); //4 (округление)
result = Math.ceil(3.3); // 4 (округление в большую сторону)
result = Math.floor(3,7); //3 (в меньшую сторону)
result = Math.sqrt(9); // 3 (квадратный корень)
result = Math.abs(-7); //7
result = Math.pow(2,2); //4 (возведение в степень)
result = Math.min(23, 45, 67, 7, 8); //7
result = Math.max(23, 56, 78, 3); //78
result = Math.random();//рандомное число от 0 до 1




console.log(result)